extends _layout
block head
  meta(http-equiv="refresh", content="10;url=/")
block content
  if title
    h1= title
  else
    h1 エラー
  p= message
  p 10秒後にトップページに戻ります。自動で戻らない場合は、以下のリンクをクリックしてください。
  a(href="/") トップページに戻る